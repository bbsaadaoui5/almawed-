specVersion: "v1"
serviceId: "my-firebase-project"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "my-firebase-project-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./example"]
